<!-- Amber pill — matches web "Fall Detection" feature pill (#fef08a / #854d0e) -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/rocky_yellow_light" />
    <stroke
        android:width="2dp"
        android:color="@color/rocky_amber" />
    <corners android:radius="100dp" />
</shape>
